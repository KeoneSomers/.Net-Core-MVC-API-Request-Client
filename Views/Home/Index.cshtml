@model IEnumerable<apiRequest.Models.StudentData>

<a asp-action="Create">Create New</a>

<ul>
    @foreach (var item in Model)
    {
        <li>
            @Html.DisplayFor(modelItem => item.Name)
            @Html.ActionLink("Details", "Details", new {id=item.Id})
        </li>
    }
</ul>